# Standalone C++ library

See [the repository README](../README.md) for dependencies, build, install and API migration.
The library can also be built directly with `cmake -S pin_ik_lib -B build-lib`.
